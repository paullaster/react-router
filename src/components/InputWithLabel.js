import React from 'react'

const InputWithLabel = ({children}) => {
  return (
    <div>
        <label>
            {children}
            
        </label>
    </div>
  )
}

export default InputWithLabel